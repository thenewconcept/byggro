<div class="bg-white overflow-hidden shadow rounded-md divide-y divide-gray-20 ">
  <div class="py-2 px-4">
    <h1 class="font-bold text-2xl my-2">Uppdatera utgift</h1>
    <h2 class="text-1xl my-2"><%= @project.title %></h2>
    <div class="mx-auto w-full">
      <turbo-frame id="edit-<%= dom_id(@expense) %>" target="_top">
        <%= render partial: "form", locals: { expense: @expense } %>
      </turbo-frame>
    </div>
  </div>
</div>