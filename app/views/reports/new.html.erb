<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-2xl">Ny rapport</h1>
  <%= turbo_frame_tag "new_report" do %>
    <%= render partial: "form", locals: { report: @report, turbo_target: dom_id(@report.checklist) } %>
  <% end %>
</div>