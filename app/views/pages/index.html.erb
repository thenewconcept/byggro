<div class="bg-white overflow-hidden shadow rounded-md divide-y divide-gray-200 p-4">
  <div class="relative max-w-lg mx-auto divide-y-2 divide-gray-200 lg:max-w-7xl p-4">
    <div>
      <h2 class="text-3xl tracking-tight font-extrabold text-gray-900 sm:text-4xl">Dokument & Instruktioner</h2>
      <div class="mt-3 sm:mt-4 lg:grid lg:grid-cols-2 lg:gap-5 lg:items-center">
        <p class="text-xl text-gray-500">Samlade dokument, policies och instruktioner för Bullerbymålarna.</p>
      </div>
    </div>

    <div class="my-6 pt-10 grid gap-16 lg:grid-cols-2 lg:gap-x-5 lg:gap-y-12">
    <% @pages.each do |page| %>
      <div>
        <p class="text-sm text-gray-500 capitalize italic">
          Uppdaterad <time><%= l page.updated_at, format: :short %></time>
        </p>
        <%= link_to page do %>
          <p class="text-xl mt-1 font-semibold text-gray-900"> <%= link_to page.title, page %> </p>
          <!--<p class="mt-3 text-base text-gray-500">Illo sint voluptas. Error voluptates culpa eligendi. Hic vel totam vitae illo. Non aliquid explicabo necessitatibus unde. Sed exercitationem placeat consectetur nulla deserunt vel. Iusto corrupti dicta.</p>-->
        <% end %>
        <div class="mt-3">
          <%= link_to 'Öppna dokument', page, class: 'text-sm font-semibold text-indigo-600 hover:text-indigo-500' %>
        </div>
      </div>
    <% end %>
    </div>
    <%= link_to icon_text('Nytt dokument', 'plus'), new_page_path, class: 'btn-primary' if policy(:page).create? %>
  </div>
</div>