<%= form_with(model: [:project, :checklist, todo], class: "contents", data: { 'turbo-frame': dom_id(todo.project) }) do |f| %>
  <%= f.text_field :description, required: true %>
  <%= f.submit %>
<% end %>