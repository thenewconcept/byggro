<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-2xl">Ny uppgift</h1>
  <%= turbo_frame_tag "new_todo" do %>
    <%= render partial: "form", locals: { todo: @todo, turbo_target: dom_id(@todo.checklist) } %>
  <% end %>
</div>