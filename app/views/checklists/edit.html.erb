
<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-2xl">Uppdatera lista</h1>
  <%= turbo_frame_tag dom_id(@checklist, :title) do %>
    <%= render partial: "form", locals: { checklist: @checklist, turbo_target: dom_id(@checklist, :title) } %>
  <% end %>
</div>