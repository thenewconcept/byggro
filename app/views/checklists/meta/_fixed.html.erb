<%= icon_badge "#{number_to_currency(reportable.bonus_fixed)}", 'banknotes', class: 'green' if policy(reportable).meta? %>
<%= icon_badge "#{reportable.hours_estimated} tim", 'flag', class: 'indigo' if not reportable.hours_estimated.zero? and policy(reportable).create? %>
<%= icon_badge("#{number_to_currency(reportable.amount)}", 'shield-check', class: 'silver') if policy(reportable).create? %>