<%#= render partial: "projects/admin/#{project.bonus}", locals: { project: project } %>

<% project.checklists.each do |checklist| %>
  <%= render partial: "checklists/admin/#{checklist.bonus}", locals: { reportable: checklist } %>
<% end %>
  
<hr/>