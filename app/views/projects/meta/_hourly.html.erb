<%= icon_badge "~ #{reportable.hours_reported} tim", 'clock', class: 'green' %>
<%= icon_badge "#{reportable.hours_estimated} tim", 'flag', class: 'indigo' if !reportable.hours_estimated.zero? and (Current.user.is_worker? or Current.user.is_manager?) %>